## Overview
- Repo: OdbDesign (C++17). Core library parses ODB++ archives into domain models/Protobuf, server exposes REST (Crow) and new gRPC endpoints. DesignCache provides cached FileArchive/Design access backed by test data under `test_data/FILES`.

## Architecture
- Library: `OdbDesignLib` contains FileModel (parsing, FeatureRecord, SymbolName, FeaturesFile), ProductModel, protobuf bindings under `protoc/`.
- Server: `OdbDesignServer` (Crow REST + gRPC). gRPC service implemented in `OdbDesignServer/Services/OdbDesignServiceImpl.{h,cpp}` with endpoints `GetDesign`, `GetLayerFeaturesStream`, `GetLayerSymbols`, `HealthCheck`.
- Tests: `OdbDesignTests` built with gtest; fixtures in `OdbDesignTests/Fixtures` provide DesignCache backed by sample design archives. CMake target `OdbDesignTests`.

## User Defined Namespaces
- (none yet)

## Components
- `OdbDesignServer/Services/OdbDesignServiceImpl`: gRPC service using DesignCache to serve designs, stream layer features, and assemble ordered symbol lists with unit normalization and strict contract checks.
- `OdbDesignLib/FileModel/Design/SymbolName`: Parses symbol records (`$index name unit`), exposes name/index/unit and protobuf conversions.

## Patterns / Notes
- Symbol ordering in `GetLayerSymbols`: normalize units → validate symbol unit compatibility → compute max referenced sym nums (sym_num/apt_def_symbol_num) with contract check → order symbols (explicit indices first, keep first on duplicates; fill gaps with index-less symbols) → pad with placeholders to cover referenced IDs.
- Symbol parsing errors: malformed index tokens now raise `parse_error` instead of silently defaulting.

- PR 563 (k3s VM cluster): scripts/k3s-cluster.ps1 manages Install/Start/Stop/Restart/Status/Uninstall inside the Debian 13 VM with `$PSNativeCommandUseErrorActionPreference = $true`; deploy.ps1 pins KUBECONFIG and pre-flights PV hostPath immutability; validate-grpc-exposure.ps1 resolves cluster kind post-context-switch and reads kubectl JSON StrictMode-safely (PSObject.Properties).
- Loki (chart 7.3.0 single-binary): retention_period enforced only with `loki.compactor.retention_enabled: true`. kube-prometheus-stack 88.5.0 + vendored grafana 12.11.0 render host-less ingress rules when `hosts: []` — values-prom.yaml host-less design is valid.
- Swagger spec version stays "0.9" until a coordinated server bump; ConfigMap manifest committed at deploy/kube/OdbDesignServer-SwaggerUI/swagger-spec-configmap.yaml, regenerated by deploy.ps1 before apply.
- TLS/HTTPS: trusted ingress TLS never existed in any k8s deploy of this repo — k3d's `--tls-san` args were for the kube-apiserver cert (6443), not the ingress; the May-2024 cert-manager/LE config was staging-only, bare-hostname (`precision5820`), and disabled next day. Current k3s serves Traefik's default self-signed cert on 443; gRPC on 50051 is plaintext (ServiceLB, not behind ingress). Options A–F + gRPC TLS + overlay layout documented in `docs/plan/https-tls-options.md` (decision OPEN). Maintainer constraint: no vendor lock-in (e.g. no Tailscale default) — repo default stays plain HTTP, TLS is a per-environment overlay.
