---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  #name: task-pv-claim
  name: k3d-volume-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
